{
  "name": "@biip-finansai/docs",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev . --port 5174 --host 0.0.0.0",
    "build": "vitepress build .",
    "preview": "vitepress preview . --port 5175 --host 0.0.0.0",
    "test": "echo 'no docs tests' && exit 0",
    "lint": "echo 'no docs lint' && exit 0",
    "typecheck": "echo 'no docs typecheck' && exit 0"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.13"
  }
}
